# 2026-04-03 工作日志

## WorkerClaw v0.14.0 发布 — OpenCLI 公共 API 工具集成

### 新增文件
- `src/agent/opencli-tools.ts` — 封装 26 个 OpenCLI PUBLIC API 命令为 WorkerClaw 内置工具

### 修改文件
- `src/agent/tool-registry.ts` — 注册 OpenCLI 工具和 web_cli 代理工具
- `package.json` — 版本号 0.13.30 → 0.14.0

### 智工坊平台修改
- `server.js` — 新增 `/api/cli/:site/:cmd` 代理 API 路由（180行新代码）

### 功能说明
1. **26 个 OpenCLI 内置工具**（权限: limited）:
   - Hacker News: top, new, best, ask, show, jobs, search
   - Stack Overflow: hot, search
   - DEV.to: top, tag
   - Lobsters: hot, newest, active
   - V2EX: hot, latest
   - Reddit: hot, search
   - Product Hunt: today
   - Steam: top-sellers
   - ArXiv: search
   - HuggingFace: trending
   - Wikipedia: search, summary, trending
   - 36kr: hot

2. **web_cli 通用代理工具** — 通过平台代理 API 调用任意 CLI 命令
   - 参数: site (必填), command (必填), query (搜索关键词), limit (条数)

3. **平台代理 API**:
   - `GET /api/cli/:site/:cmd?q=xxx&limit=20` — 执行 CLI 命令
   - `GET /api/cli` — 列出所有可用命令
   - 18 个服务端代理命令实现

## 阶段三架构复盘
- 当前实现属于"过渡版"：平台路由本质上还是 `fetch` handler 映射，尚未实现真正的三引擎分发（fetch / browser / auth）。
- 确认后续正确方向是平台中心化 `web_cli`：Agent 端尽量只保留通用 HTTP 调用工具，浏览器池、登录态、缓存、限流、审计放在平台侧。
- 若进入下一步开发，优先重构平台侧命令注册表与执行引擎抽象，再接浏览器池和塘主登录态注入。

## 阶段三 v2 实现（真正阶段三）
- 创建 `routes/cli.js` (700+ 行) — 命令注册表 + 三引擎分发架构
- 从 `server.js` 移除旧的 `CLI_PROXY_HANDLERS` (~180 行)
- 平台侧核心组件：
  - `CommandRegistry` — 声明式命令注册
  - `FetchEngine` / `BrowserEngine` / `AuthEngine` — 三引擎
  - `CliCache` / `CliRateLimiter` / `CliAuditLogger` — 缓存/限流/审计
  - `CliDispatcher` — 核心调度器
- 新增 API: POST /api/cli/execute, GET /api/cli/commands, 凭据同步/撤销
- 新增数据库表: owner_credentials, cli_audit_log, cli_jobs
- WorkerClaw v0.15.0: web_cli 重构 + web_cli_describe 命令发现工具

## 阶段三后续完善
- **BrowserEngine 升级**: 共享浏览器实例 + Context Pool 复用 + 按塘主隔离 + 空闲自动清理 + actions 多步操作链
- **更多 auth 命令**: 新增知乎 profile/search、B站 profile/hot/search、Twitter profile/post
- **Chrome 扩展**: `chrome-extension/` 目录 — manifest.json + popup.html + popup.js + background.js
  - 支持一键同步微博/知乎/B站/Twitter Cookie 到智工坊
  - 自动检测登录态、凭据撤销
- **CLI 管理面板**: `public/cli-dashboard.html` — 凭据管理 + 审计日志 + 命令列表 + 系统状态
- **凭据验证 API**: `POST /api/cli/credentials/verify/:id` (微博/知乎/B站自动验证)
- **WorkerClaw v0.15.1**: 26 个 OpenCLI 直连工具默认不注册 (debug-only)，设 `WORKERCLAW_DEBUG_TOOLS=true` 启用
- 总计 27 个平台命令: fetch=22, browser=3, auth=8（含微博/知乎/B站/Twitter）

## Chrome 扩展认证重构 + cli.js bug修复
- **bug**: `routes/cli.js` 中 6 处用 `req.userId`，但 JWT 中间件设置的是 `req.botId`，导致凭据 API 永远返回 401
- **修复**: `req.userId` → `req.botId`（涉及 credentials/sync, credentials GET, credentials/:id DELETE, audit, verify, execute context）
- **扩展重构**: popup 改为 X-Bot-Id + X-Bot-Token header 认证（存 chrome.storage.local），不再依赖 Cookie
  - 设置页添加 Bot ID / Token 输入框 + 验证/登出按钮
  - tab 切换改用 addEventListener（修复"设置"点不动的问题）
  - 所有 API 请求带 X-Bot-Id/X-Bot-Token headers

## Chrome 扩展持续完善
- **塘主/虾双模式认证**: 从网页获取时识别 bot_jwt(塘主用Bearer) 和 bot_private_key(虾用X-Bot-Token)
- **同步按钮修复**: innerHTML onclick → addEventListener（Manifest V3 作用域问题）
- **Cookie 域匹配修复**: chrome.cookies.getAll 同时查 domain 和 .domain（修复微博子域 cookie 获取失败）
- **图标替换(v1.2.0)**: 扩展图标改为智工坊 favicon 虾图标；平台图标改为各网站 favicon (微博/知乎/B站/Twitter)
- **安装跳转修复**: 从插件页改为 miniabc.top 首页
- **v2.0.0 大重构**:
  - **认证修复**: 旧方案从网页读 bot_jwt 直接用，但 JWT 7天过期导致"认证失败"
  - **新方案**: 从网页获取 bot_public_key → 调 `/api/auth/refresh-jwt` 换取新 JWT；保存 botPublicKey 支持后台自动刷新
  - **UI 简化**: 去掉设置 Tab 和手动输入方式，"从智工坊网页获取"作为唯一登录入口，放在最显眼位置
  - 去掉 serverUrl 配置项，硬编码 `https://www.miniabc.top`

## WorkerClaw v0.16.0 — 私有虾消息处理重构

### 问题
- 私有虾收到主人私信时，回复"你可以发个任务给我"（打工虾逻辑）
- 私有虾收到外部人员私信时，没有拒绝

### 设计
- **`mode: 'private'` 私有虾只认主人**（`rentalState.renterId`，从服务器拉取）
- **主人发私信** → 直接用 AgentEngine 执行指令，不走任务流
- **外部人员发私信** → 礼貌拒绝"我是主人专属内勤虾"
- **聊天室消息** → 私有虾完全不参与
- **ownerId 安全设计**: 不可本地配置，仅从服务器 `checkRentalStatus()` 拉取 `renterId`
  - 防止出租者篡改主人身份冒用浏览器登录态发微博等

### 修改文件
- `src/task/task-manager.ts`:
  - 新增 `isPrivateMode()`, `isOwner()`, `handleOwnerDirectMessage()`, `handlePrivateExternalMessage()`
  - `handlePrivateMessageReply()`: 私有虾模式分流（主人→直接执行 / 外部→拒绝）
  - `handleChatMessageReply()`: 私有虾直接 return 不参与聊天室
- `src/core/config.ts`: 无改动（ownerId 不加入本地配置）
- `src/core/workerclaw.ts`:
  - `isPrivateMode()` 判断扩展：`config.mode === 'private'` OR `rentalState.active`
  - 被租用时自动停止 behaviorScheduler，租赁到期后自动恢复（仅限原 public 虾）

## 智工坊用户手册
- 创建 `/Users/admin/WorkBuddy/20260310201629/public/docs/user-guide.html`
- 面向普通用户的使用说明，涵盖：平台角色、任务系统、租用系统、社交功能、等级体系、常见问题
- 使用平台统一导航栏和视觉风格（品牌色 #667eea）
