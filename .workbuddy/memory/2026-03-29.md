# 2026-03-29 工作日志

## README 文档更新
- 将 Docker 部署方式写入 README.md，标记为"推荐"方式
- 添加完整的 Docker 部署步骤（clone → init-config.sh → compose up）
- 添加 Docker 目录结构说明和常用命令
- 补充国内服务器加速提示（清华镜像 + 腾讯云 Docker 镜像）
- 原"快速开始"改为"手动安装"子节
- "配置示例"改为"配置说明"，用表格列出核心配置项，说明环境变量语法
- 补充 Phase 5（浏览器技能）和 Phase 6（任务估价）到功能特性列表

## 网站文档更新
- 更新 `/Users/admin/WorkBuddy/20260310201629/public/workerclaw.html`
- 原安装流程拆分为「方式一：Docker 部署（推荐）」和「方式二：手动安装」
- Docker 部署：3步流程（clone → init-config.sh → compose up）+ 加速提示 + 常用命令
- 手动安装：4步流程（安装 → 配置 → 浏览器依赖 → 启动）
- 管理与更新合并为一个区块，区分 Docker 和手动两种方式
- FAQ 新增 Docker 相关问题（区别、构建慢、日志查看）
- 修正配置示例中的 wsUrl 为 `/ws/openclaw`

## 部署到服务器
- WorkerClaw: 服务器目录从手动复制改为 git clone (`/root/workerclaw`)，保留 .env 和 docker-data/config.json
- WorkerClaw Docker: `docker compose up -d --build` 全部缓存命中，启动正常，认证成功
- 智工坊网站: `git checkout -- . && git clean -fd && git pull` 清理本地冲突后 Fast-forward 拉取，PM2 重启成功
- 网站 `git config --global --add safe.directory /root/bot-social` 之前需要先设置
