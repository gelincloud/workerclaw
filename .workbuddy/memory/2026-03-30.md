# 2026-03-30 工作日志

## init-config.sh 修复
- 修复同步名称到平台时缺少 `X-Bot-Id` header 导致 `AUTH_REQUIRED` 的问题
- 优化同步失败提示，告知用户名称会在容器启动后自动生效
- 已推送到 GitHub (`2b7e451`)

## 服务端 JWT_EXEMPT_ROUTES 修复
- **问题**: agent 用户发送聊天室消息和私信时，被全局 `jwtAuthMiddleware` 拦截返回 `AUTH_REQUIRED`
- **根因**: `POST /api/chat/messages` 和 `POST /api/private-messages` 被 `app.use('/api', jwtAuthMiddleware)` 拦截，但这些路由自身已有 botId/senderId 校验，不需要 JWT
- **修复**: 将 `/chat/messages` 和 `/private-messages` 加入 `JWT_EXEMPT_ROUTES`
- **部署**: 通过 SCP 直接传 server.js 到生产服务器并 pm2 restart
- 注意: miniabc-web 仓库的 commit `962a14a` 还未 push 到 GitHub（网络超时）

## workerclaw 客户端改进
- `sendChatMessage` 增加 WS 未注入时的 debug 日志（`55486d7`）

## 智能活跃行为完善 + 技能同步 (`815a998`)
- 新增 `blog_comment` 行为类型，接入调度器（LLM 生成评论内容 + 随机选博客评论）
- 频率限制：blog(1h间隔/天3次) + blog_comment(30m间隔/天10次)
- `PlatformApiClient` 新增 `updateSkills()` 方法，启动后自动同步内置技能到平台
- `status` 命令智能活跃默认显示为已启用（`?? true`）

## ConcurrencyController 崩溃修复 (`663eac1`, v0.11.2)
- **根因**: `mergeConfig` 对 task 子对象浅合并，当用户配置覆盖 task 但不含 concurrency 时，concurrency 变为 undefined
- **修复**: `ConcurrencyController` 构造函数加 DEFAULT_CONFIG 兜底 + `mergeConfig` 对 concurrency/evaluation/pricing/timeout 深合并
- **现象**: 收到任务时 evaluateTask→getStats() 崩溃，任务状态卡在 evaluating

## Docker 自动更新 + 密钥找回 (`2951c2c`/`ed2f243`)
- Docker entrypoint 启动时自动对比 npm 版本更新（`AUTO_UPDATE=true/false` 控制）
- 智工坊新增邮箱找回密钥功能（6个新API + login.html + profile.html）
- 前提条件：塘主账号已完成实名认证且绑定了邮箱

## 邮箱绑定 401 修复
- **问题**: profile.html 中 `sendBindEmailCode()` 和 `verifyBindEmail()` 缺少认证 header，被 JWT 拦截
- **修复**: 在两个 fetch 请求中加上 `getAuthHeaders(true)`
- **结果**: 邮箱绑定功能恢复正常

## 租赁模式系统实施 (已完成 10/10 ✅)
- **server.js**: 新增 bot_rentals 表、bots 表 rental_mode/last_heartbeat/client_type 字段
- **服务端 API**: 5个端点（创建/查询状态/我的租赁列表/取消）+ 私信拦截 + 免费任务
- **定时任务**: 到期检查(5min) + 离线检测(2min, 心跳丢失10min阈值+5min确认窗口)
- **离线终止**: 按实际在线时长结算费用，剩余退回塘主，WebSocket通知双方
- **前端 profile.html**: 租赁弹窗(选时长/显示价格) + 三种状态 + bot类型标识(🦐 WorkerClaw / 🐙 OpenClaw) + 租赁按钮仅 WC 显示
- **前端 xiaxin.html**: 免费任务(隐藏报酬输入)、?open=botId 跳转、免费任务气泡🎁、聊天导航/头部显示 bot 类型
- **WorkerClaw v0.12.0**: rental_started/rental_expired WS 消息处理、启动时检查租赁状态、认证上报 clientType、RentalState 接口、checkRentalStatus API、getStatus 包含租赁状态
- **部署验证**: 服务端 ✅, WorkerClaw npm v0.12.0 ✅, GitHub push ✅

## 租赁价格调整
- **旧价格**: 按小时计价（1天¥120/3天¥324/7天¥672/30天¥2160）
- **新价格**: 1天¥10 / 3天¥20 / 7天¥30 / 30天¥60，自定义时长¥0.5/小时
- **改动**: server.js（RENTAL_PRICING套餐固定价 + getRentalPrice函数）+ profile.html（按钮价格 + JS计算逻辑）
- **部署**: SCP + pm2 restart ✅

## 数据库版本追踪与自动备份
- **server.js 新增**:
  - `CURRENT_SCHEMA_VERSION = 2` 常量（每次大改表结构时递增）
  - `backupDB(reason)` — 迁移前自动备份到 `data/bots.db.bak.{timestamp}.sql`，保留最近5份
  - `getDBSchemaVersion()` / `recordSchemaMigration()` — schema_version 表追踪版本
  - `initDB()` 末尾：检测版本差异→自动备份→执行迁移→记录版本
- **deploy-server.sh**: 本地一键部署脚本（自动备份DB→上传文件→重启PM2→验证服务→回滚提示）
- **用法**: `./deploy-server.sh server.js` 或 `./deploy-server.sh server.js profile.html`
- **首次运行**: v0→v2 迁移，备份文件 `bots.db.bak.2026-03-30T07-47-17.sql` ✅

## session.js auth fallback 修复
- **问题1**: `/api/auth/my-email` 401 — agent 用户没有 JWT，`getAuthHeaders(true)` 只传 JWT 的 Bearer，不传 bot token
- **问题2**: `jwtAuthMiddleware` 对 GET 请求直接放行但不设 `req.botId`
- **修复1**: `jwtAuthMiddleware` — GET 请求也尝试 JWT 和 bot token 认证（不拦截，但设 `req.botId`）
- **修复2**: `getAuthHeaders(true)` + `authFetch` — 无 JWT 时 fallback 到 `X-Bot-Id` + `X-Bot-Token`
- **租赁500**: 旧备份恢复时表结构不一致导致，当前数据库已正确（17列），重启后无新错误

## 租赁脏数据清理
- 上次INSERT失败导致的4条脏租赁记录(status=active但交易未写入)，冻结了全部¥20余额
- 清理: 删除4条脏记录 + 释放frozen_balance=0 → 可用余额恢复¥20.00

## WorkerClaw v0.12.1 — config mergeConfig 修复
- **根因**: CLI start 命令直接 `createWorkerClaw(config)` 没有用 `mergeConfig` 合并默认值
- 用户 config.json 的 task 只有 `autoAccept`，没有 `concurrency/evaluation/pricing/timeout`
- 导致 `config.task.concurrency` 为 undefined，evaluateTask 访问 `.maxConcurrent` 报 TypeError
- **修复**: start 命令中先 `mergeConfig({}, config)` 再传给 createWorkerClaw
- npm 发布 v0.12.1，Docker 容器自动更新成功

## 智工坊介绍页面
- 新建 `/about.html` — 深色渐变背景，介绍平台概述、9大核心功能、双角色体系、任务流程、等级系统、快速开始
- 登录页底部新增「📖 了解智工坊」链接（紫色，位于OpenClaw指南和QQ群之间）
- **根因**: `server.js:9987` 租赁创建时的 `balance_transactions` INSERT 语句列数(11)和值数(10)不匹配
- **具体**: VALUES 中 `datetime('now','localtime')` 缺少独立占位符，导致 description 没有对应值
- **修复**: VALUES 末尾补一个 `?`，让 created_at 正确映射到 `datetime('now','localtime')`
- **部署**: sed 修复 + pm2 restart ✅

## WorkerClaw v0.12.2 — 工具注册表 Bug 修复
- **根因**: `AgentEngine` 构造函数调用 `createDefaultToolRegistry()` 但 `tool-registry.ts` 导出的函数名是 `createBuiltinToolRegistry`，名称不匹配导致导入为 undefined
- 空的 ToolRegistry 没有注册任何内置工具，所有工具调用都被 `isToolAllowed` 拒绝
- **修复**: 将 `createBuiltinToolRegistry` 统一重命名为 `createDefaultToolRegistry`（tool-registry.ts + task-manager.ts）
- npm 发布 v0.12.2，Docker 容器已更新确认 ✅

## WorkerClaw v0.12.3 — 内置工具执行器实现
- **根因**: v0.12.2 修复了工具注册表名称匹配，但内置工具(web_search/read_file/write_file)只有定义没有执行器，执行时返回 `not_implemented` 占位文本
- **问题链**: LLM 不调用工具(纯文本回复) → 质量审核失败 → 自动降级 → browser_extract(Playwright未安装) → web_search(not_implemented) → 最终提交了无意义的 JSON
- **修复**:
  - `web_search` 执行器: 使用 DuckDuckGo Lite 免费 HTML 搜索 API，解析搜索结果返回标题/链接/摘要
  - `read_file` 执行器: 读取本地文件(带安全边界检查，限1MB)
  - `write_file` 执行器: 写入本地文件(自动创建目录)
  - `autoFindImage` 优化: 浏览器不可用时跳过直接走 `autoSearchForImageUrls`(用 web_search 搜图片URL返回链接)
  - 新增 `autoSearchForImageUrls` 方法: 搜索图片关键词 → 从结果提取图片 URL → 返回搜索结果+图片链接
- npm 发布 v0.12.3，Docker 容器已更新确认 ✅
- **注意**: Docker 容器中 Playwright 浏览器未安装，browser_extract/browser_navigate 工具不可用

## WorkerClaw v0.12.4 — Playwright 浏览器适配
- **根因**: `browser-session.ts` 的 `chromium.launch()` 没有指定 `executablePath`，Playwright 默认找自己下载的浏览器（`~/.cache/ms-playwright/`），但 Dockerfile 设置了 `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` 跳过了下载
- **Docker 实际已安装系统级 Chromium**: `/usr/bin/chromium` (Chromium 146.0)，且 `CHROME_PATH=/usr/bin/chromium` 环境变量已设置
- **修复**: `launchBrowser()` 方法新增环境变量检测，优先使用 `CHROME_PATH` 或 `PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH` 作为 `executablePath`，没有则走 Playwright 默认
- npm 发布 v0.12.4，Docker 容器已更新确认，浏览器工具现在可用 ✅

## WorkerClaw v0.12.5 — 找图任务全链路修复
- **问题1**: browser_extract 静默失败 — `extractStructured`/`takeScreenshot` 的 catch 块没有 logger.error，异常原因不可见
- **问题2**: Unsplash/Pexels 是 SPA，`domcontentloaded` 时内容未渲染完，extract 返回空数据
- **问题3**: 自动降级调用 web_search 时 remainingMs 耗尽（browser_extract 用了 7-9 秒），executeWithTimeout 直接抛异常
- **问题4**: 找图指南推荐用 browser_extract（elevated 权限），但 LLM 是 standard 权限看不到该工具，导致放弃调用任何工具
- **修复**:
  - `browser-sandbox.ts`: extractStructured/takeScreenshot catch 块添加 logger.error
  - `browser-sandbox.ts`: extractStructured 增加 `waitForLoadState('networkidle')` + 2s 额外等待 JS 渲染
  - `agent-engine.ts`: autoBrowserExtractImage/autoSearchForImageUrls/autoSearch 降级调用时重置 receivedAt 确保超时窗口充足
  - `agent-engine.ts`: 找图指南重写，强调必须先调用 web_search（limited 权限可用），更强硬要求 LLM 必须调用工具
- npm 发布 v0.12.5，Docker 容器已更新确认 ✅

## about.html 添加经验基因系统介绍
- 在核心功能网格中新增"经验基因系统"特性卡片
- 在等级系统和快速开始之间新增完整的经验基因系统详细介绍板块
- 包含 6 个子功能卡片：经验基因(Gene)、经验胶囊(Capsule)、信号检测器、智能搜索引擎、经验 Hub、自动进化闭环
- 已部署到服务器 miniabc.top/about.html

## 关于页新增安全与信任板块
- 在"快速开始"之前插入"安全与信任"精简板块
- 精炼展示 workerclaw-security.html 的核心内容：四层安全门、五大沙箱、权限分级与金额保护、输出安全审查、资源限制、开源透明
- 每个能力用 feature-card 展示，底部链接到完整安全策略文档
- 同时在登录页和关于页 footer 添加了联系邮箱 glin_1@qq.com

## 角色概念同步与文档修正
- 平台概述重写：明确三层角色概念（塘主=金主付钱方、养虾人=运营赚钱方、打工虾=Agent实例）
- "双角色体系"改为"角色体系"，从两栏改为三栏，新增养虾人角色卡片
- 塘主描述调整：移除"参与仲裁评审"和"部署虾代为活跃"，聚焦发任务+租虾
- 养虾人描述：部署虾、可多虾、用Token登录网页查看收益、提现到微信、也可发任务
- 打工虾描述：移除"私聊引导发任务"，聚焦接单执行+社交
- 仲裁系统：从"社区参与评审"改为"AI Agent 自动参与评审"
- Agent 租赁：从"租塘主的虾"改为"租养虾人的虾"
- 任务流程结算：从"结算到虾主人"改为"结算到打工虾账号"

## workerclaw token CLI 命令
- 新增 `workerclaw token` 子命令，读取配置文件输出 botId + 完整 token
- 支持 -c 参数指定配置文件路径（Docker: /app/data/config.json，本地: ~/.workerclaw/config.json）
- npm v0.12.6 已发布
- workerclaw.html 文档已同步更新：手动安装和 Docker 命令列表加入 token，重写 Token 获取提示

## 网站仓库同步
- 服务器 /root/bot-social 提交 ba456eb（8个文件：about.html新增、login/workerclaw/profile/xiaxin/server.js/deploy.sh/session.js 更新）
- 通过 git bundle 传输到本地（服务器到 GitHub 网络不通），本地 force push 到 GitHub
- 本地仓库 /Users/admin/WorkBuddy/20260310201629 已同步到最新，工作区干净
- 今后修改网站应直接在本地仓库操作，改完提交推送即可

## 三项目代码架构全面熟悉
- 完成智工坊生态三个项目的代码架构分析：
  1. **智工坊网站 (MiniABC)**: 单文件 server.js (4092行)，Express+SQLite+COS+WS，60+ REST API，原生 HTML/JS 前端(30个HTML页面)
  2. **OpenClaw 插件**: 17个TS文件，Channel插件架构(TaskManager+TaskExecutor+ActiveBehaviorManager+安全审查+性格系统+目标系统)
  3. **WorkerClaw 框架**: 67个TS文件，五层架构(接入→安全→任务→Agent引擎→技能)，含沙箱/经验基因/Playwright/CLI
- 生态关系：网站是平台(数据中心)，插件是桥梁(OpenClaw个人虾模式)，框架是独立Agent(公域模式)

## WorkerClaw v0.12.7 — 找图任务降级链全面修复
- **背景**: glm-4.7-flash 模型首轮未调用工具（纯文本编造URL），质量审核触发自动降级，但降级链三环全部失败
- **故障链**:
  1. browser_extract(Unsplash/Pexels) → `Cannot read properties of undefined (reading 'text')` — SPA 页面 evaluate 返回 undefined
  2. web_search(DuckDuckGo) → `success: false` — Docker 容器网络可能被 DuckDuckGo 封锁
  3. autoFindImage 返回 null → 任务直接报 failed
- **修复 (3个文件)**:
  - `browser-sandbox.ts`: extractStructured null check(|| {}兜底) + 空页面保护 + extractFn 内 try-catch + 图片提取优化(srcset + 过滤 data: URI)
  - `tool-registry.ts`: web_search 添加 Bing 备用搜索引擎(DuckDuckGo失败自动切Bing) + 三层 DuckDuckGo 解析正则 + 更真实的 UA
  - `agent-engine.ts`: autoFindImage 降级链扩展为4步(browser_extract→截图图库页面→web_search→搜索建议文本兜底) + autoBrowserScreenshot 多源尝试(Pixabay优先) + 所有降级方法重置超时(receivedAt) + 截图路径多正则匹配 + 最终兜底返回completed而非null
- 编译通过，npm v0.12.7 已发布

## WorkerClaw v0.12.8 — 降级链审查修复 + Bing解析修正
- **问题1**: Bing 搜索返回格式异常 — URL 粘在一起（`baidu.comhttps://baike.baidu.com`），title 字段包含域名文本
  - **根因**: Bing 的 `<h2><a>` 结构内嵌套 `<cite>` 等元素，原正则贪婪匹配把域名文本也当成了 title
  - **修复**: 优先从 `<h2>` 标签中提取只取 `<a>` 内纯文本，过滤 `javascript:` 链接和超长标题
- **问题2**: 降级链最终兜底跳过了质量审查，纯文字直接提交 completed
  - **根因**: v0.12.7 的降级链在 autoFindImage 最终兜底返回 `status: 'completed'` + 纯文字，从 tryAutoRemediation 返回后直接提交，绕过了 checkOutputQuality
  - **修复**: tryAutoRemediation 返回前增加审查：找图任务必须 outputs 中有 image 类型才返回 completed；autoFindImage 最终兜底改为返回 null（走上层 failed 路径）
- npm v0.12.8 已发布
